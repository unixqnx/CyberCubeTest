using RestSharp;
using System;
using System.Collections.Generic;
using System.Text;
using System.Threading.Tasks;

namespace DotNetCoreConsole
{
    public class PlayerService
    {
        //private const string  OPENDOTA_BASE_URL = "https://api.opendota.com"

        //public static Player GetPlayer(string userId32)
        //{
        //    Uri baseUrl = new Uri("https://api.opendota.com");
        //    Uri uri = new Uri($"api/players/{userId32}", UriKind.Relative);
        //    var client = new RestClient(baseUrl);
        //    var request = new RestRequest(uri);
        //    var response = client.Get<Player>(request).Data;
        //    return response;
        //}


        //public static async Task<Player> GetPlayerAsync(string userId32)
        //{
        //    Uri baseUrl = new Uri("https://api.opendota.com");
        //    Uri uri = new Uri($"api/players/{userId32}", UriKind.Relative);
        //    var client = new RestClient(baseUrl);
        //    var request = new RestRequest(uri);
        //    var response = await client.GetAsync<Player>(request);
        //    return response;
        //}



    }
}
